pipeline {
    agent any
      parameters {
    string(name: 'Class', description: 'Enter the Clss name')
  }

    stages {

        stage('Building') {
            steps {
                  sh '''
                    chmod +x hello.sh
                    ./hello.sh
                    echo Wellcom to Clas, ${USER}
                '''       
            }
        }
      
    }
}
